# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ViviendaU_db
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA Configuration
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.servlet.context-path=/api
server.port=8080

# Open in View
spring.jpa.open-in-view=true

# Cloudinary Configuration
cloudinary.cloud-name=dopb4ao7j
cloudinary.api-key=926531743225557
cloudinary.api-secret=LWPNTt8lAui06YIP2993_kSD9e8

# Multipart Configuration (for file uploads)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
